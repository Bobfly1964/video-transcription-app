# Stereo Recognition System

Система распознавания рукописного текста с бумаги в режиме реального времени с использованием двух веб-камер для проекта ARISTOTEL.

## 🎯 Статус проекта

**Current Version:** v0.1.0 (MVP Prototype)

- [x] Week 0: Project setup & MVP prototype
- [ ] Week 1-2: Enhanced MVP with better OCR
- [ ] Week 3-4: Camera calibration with ArUco
- [ ] Week 5+: Full feature set

## ⚡ Quick Start

### Требования

- Python 3.10+
- 1-2 USB веб-камеры
- Windows/Linux/macOS

### Установка

```bash
# 1. Клонирование репозитория
git clone https://github.com/Bobfly1964/stereo-recognition.git
cd stereo-recognition

# 2. Создание виртуального окружения
python -m venv venv

# Windows:
venv\Scripts\activate

# Linux/Mac:
source venv/bin/activate

# 3. Установка зависимостей
pip install -r requirements-minimal.txt
```

### Первый запуск

```bash
# Тестовый запуск (5 циклов)
python simple_prototype.py --interval 5 --cycles 5

# Непрерывный режим
python simple_prototype.py --interval 10

# Только одна камера
python simple_prototype.py --cam1 0 --cam2 0
```

Результаты сохраняются в `output/cycle_NNNN/`

## 📚 Возможности v0.1.0

- ✅ Захват с 1-2 веб-камер
- ✅ Автоматическая детекция изменений
- ✅ OCR распознавание (русский, английский)
- ✅ Сохранение цвета текста (RGB)
- ✅ Экспорт результатов (JSON + PNG)
- ✅ CLI интерфейс с параметрами

## 📁 Структура проекта

```
stereo-recognition/
├── simple_prototype.py        # Главный файл MVP
├── config.yaml                # Конфигурация
├── requirements-minimal.txt   # Зависимости
├── output/                    # Результаты (gitignored)
├── src/                       # Будущий модульный код
└── tests/                     # Тесты (будущее)
```

## 🎨 Примеры использования

### Базовое использование

```bash
# Запуск с дефолтными настройками
python simple_prototype.py
```

### С параметрами

```bash
# Интервал 15 секунд, максимум 10 циклов
python simple_prototype.py --interval 15 --cycles 10

# Конкретные камеры
python simple_prototype.py --cam1 0 --cam2 2

# Только одна камера
python simple_prototype.py --cam1 0 --cam2 0
```

### Просмотр результатов

Результаты сохраняются в `output/cycle_NNNN/`:
- `recognized_text.json` - распознанный текст с координатами и цветами
- `annotated_image.png` - изображение с bbox и текстом
- `original_image.png` - оригинальный кадр

## 🔧 Конфигурация

Настройки в `config.yaml`:
- Параметры камер (разрешение, FPS)
- Настройки OCR (языки, GPU)
- Порог детекции изменений
- Формат вывода

## 🐛 Troubleshooting

### Камеры не открываются

```bash
# Проверка доступных камер
python -c "import cv2; [print(f'Camera {i}: {cv2.VideoCapture(i).isOpened()}') for i in range(5)]"
```

### OCR слишком медленный

В `simple_prototype.py` измените:
```python
self.reader = easyocr.Reader(['ru', 'en'], gpu=True)  # Включить GPU
```

### Ошибки с зависимостями

```bash
# Переустановка зависимостей
pip install --force-reinstall -r requirements-minimal.txt
```

## 🚀 Roadmap

### Week 1-2
- [ ] Улучшенная детекция изменений
- [ ] Логирование в файл
- [ ] Модульная структура кода
- [ ] Unit тесты

### Week 3-4
- [ ] Калибровка камер (ArUco)
- [ ] Устранение искажений
- [ ] Перспективная трансформация

### Week 5+
- [ ] Стерео-обработка (disparity map)
- [ ] Сегментация контента (U-Net)
- [ ] REST API
- [ ] Интеграция с ARISTOTEL

## 📄 Лицензия

MIT License

## 👤 Автор

Vladimir Danilov - [@Bobfly1964](https://github.com/Bobfly1964)

Проект: [ARISTOTEL](https://github.com/Bobfly1964/ARISTOTEL) - AI-система для STEM образования

## 🙏 Благодарности

- [EasyOCR](https://github.com/JaidedAI/EasyOCR) - OCR engine
- [OpenCV](https://opencv.org/) - Computer vision library

---

**Status:** Active Development
**Version:** 0.1.0 (MVP)
**Last Updated:** 2024-11-24

# Video Transcription App

Полнофункциональное веб-приложение для транскрипции видео с возможностью фильтрации по темам и управления заметками.

## 🚀 Возможности

- **Транскрипция видео**: Поддержка YouTube и других платформ
- **Фильтрация по темам**: Автоматическая фильтрация сегментов по ключевым словам
- **Управление заметками**: Создание, редактирование и удаление заметок
- **Прогресс-бар**: Отслеживание процесса транскрипции в реальном времени
- **OAuth2 аутентификация**: Безопасная авторизация пользователей
- **Масштабируемость**: Kubernetes-ready архитектура

## 🏗️ Архитектура

### Frontend
- **React 18** + TypeScript
- **Material UI** для интерфейса
- **React Query** для управления состоянием
- **Vite** для сборки

### Backend
- **FastAPI** для API
- **Celery** + Redis для фоновых задач
- **PostgreSQL** для метаданных
- **S3/MinIO** для хранения результатов
- **OpenAI Whisper** для транскрипции

### Infrastructure
- **Docker** для контейнеризации
- **Kubernetes** для оркестрации
- **GitHub Actions** для CI/CD

## 📦 Установка и запуск

### Локальная разработка

1. **Клонируйте репозиторий**
```bash
git clone <repository-url>
cd video-transcription-app
```

2. **Настройте переменные окружения**
```bash
# Создайте .env файл в корне проекта
cp .env.example .env
# Отредактируйте .env файл с вашими настройками
```

3. **Запустите с Docker Compose**
```bash
docker-compose up -d
```

4. **Откройте приложение**
- Frontend: http://localhost:3000
- Backend API: http://localhost:8000
- MinIO Console: http://localhost:9001

### Продакшн деплой

1. **Настройте Kubernetes кластер**

2. **Создайте секреты**
```bash
kubectl create secret generic app-secrets \
  --from-literal=OPENAI_API_KEY=your-openai-key \
  --from-literal=SECRET_KEY=your-secret-key \
  -n video-transcription
```

3. **Примените манифесты**
```bash
kubectl apply -f infrastructure/k8s/
```

## 🔧 Конфигурация

### Переменные окружения

#### Backend
```env
# Database
DATABASE_URL=postgresql://user:password@localhost/video_transcription

# Redis
REDIS_URL=redis://localhost:6379

# OpenAI
OPENAI_API_KEY=your-openai-api-key

# S3/MinIO
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_BUCKET=transcriptions
S3_ENDPOINT=http://localhost:9000

# Security
SECRET_KEY=your-secret-key
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
```

#### Frontend
```env
VITE_API_BASE_URL=http://localhost:8000
```

## 📚 API Документация

После запуска backend, API документация доступна по адресу:
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

### Основные endpoints

#### Транскрипция
- `POST /api/transcribe` - Запуск транскрипции
- `GET /api/transcribe/{jobId}/status` - Статус задачи
- `GET /api/transcribe/{jobId}/result` - Результат с фильтрацией

#### Заметки
- `GET /api/notes` - Список заметок
- `POST /api/notes` - Создание заметки
- `GET /api/notes/{id}` - Получение заметки
- `PUT /api/notes/{id}` - Обновление заметки
- `DELETE /api/notes/{id}` - Удаление заметки

## 🧪 Тестирование

### Backend тесты
```bash
cd backend
pip install -r requirements.txt
pytest --cov=app
```

### Frontend тесты
```bash
cd frontend
npm install
npm run lint
npm run build
```

## 🚀 CI/CD

GitHub Actions автоматически:
1. Запускает тесты при push/PR
2. Собирает Docker образы
3. Публикует в Docker Hub
4. Деплоит в Kubernetes

### Требуемые секреты GitHub
- `OPENAI_API_KEY` - OpenAI API ключ
- `DOCKER_USERNAME` - Docker Hub username
- `DOCKER_PASSWORD` - Docker Hub password
- `KUBE_CONFIG` - Base64 encoded kubeconfig

## 📁 Структура проекта

```
video-transcription-app/
├── frontend/                 # React приложение
│   ├── src/
│   │   ├── components/       # React компоненты
│   │   ├── pages/           # Страницы приложения
│   │   ├── services/        # API сервисы
│   │   └── ...
│   ├── package.json
│   └── vite.config.ts
├── backend/                  # FastAPI приложение
│   ├── app/
│   │   ├── core/            # Конфигурация и база данных
│   │   ├── models/          # Pydantic схемы и SQLAlchemy модели
│   │   ├── routers/         # API роутеры
│   │   ├── services/        # Бизнес-логика
│   │   └── tasks/           # Celery задачи
│   ├── requirements.txt
│   └── main.py
├── infrastructure/           # Инфраструктура
│   ├── docker/              # Docker файлы
│   └── k8s/                 # Kubernetes манифесты
├── .github/workflows/        # GitHub Actions
├── docker-compose.yml        # Локальная разработка
└── README.md
```

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект лицензирован под MIT License - см. файл [LICENSE](LICENSE) для деталей.

## 🆘 Поддержка

Если у вас есть вопросы или проблемы:
1. Проверьте [Issues](https://github.com/your-repo/issues)
2. Создайте новый Issue с описанием проблемы
3. Обратитесь к документации API

## 🔮 Планы развития

- [ ] Поддержка большего количества видео платформ
- [ ] Улучшенная фильтрация с использованием AI
- [ ] Экспорт заметок в различные форматы
- [ ] Мобильное приложение
- [ ] Интеграция с облачными сервисами 